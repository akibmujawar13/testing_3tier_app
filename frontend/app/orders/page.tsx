'use client'; import {useState} from 'react'; const base=process.env.NEXT_PUBLIC_API_BASE_URL||'http://localhost:8080'; export default function Orders(){const [msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();const f=new FormData(e.target);const r=await fetch(base+'/api/v1/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({customerId:Number(f.get('customer')),items:[{productId:Number(f.get('product')),quantity:Number(f.get('quantity'))}]})});setMsg(r.ok?'Order created successfully.':'Unable to create order: '+r.status)}return <><section className="title"><div><h1>Create order</h1><p>Runs inventory checks, transactional persistence, and a traceable workflow.</p></div></section><form className="orderForm panel" onSubmit={submit}><label>Customer ID<input name="customer" type="number" min="1" defaultValue="1"/></label><label>Product ID<input name="product" type="number" min="1" defaultValue="1"/></label><label>Quantity<input name="quantity" type="number" min="1" defaultValue="1"/></label><button>Create order</button>{msg&&<p>{msg}</p>}</form></>}
