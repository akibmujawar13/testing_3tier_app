# Database

Create the database/user, then run `schema.sql` followed by `seed.sql`. The `scripts/seed-db.sh` helper does this using the values in `.env`. Seed data is deterministic: 120 customers, 125 products, and 240 orders.
